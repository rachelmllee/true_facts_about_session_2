<%= bootstrap_form_for(@person) do |f| %>
	<% if @person.errors.any? %>
	    <div class="error_message">
	      <h3>
	        <%= pluralize(@person.errors.count, "error") %> prohibited
	        this person from being saved:
	      </h3>
	      <ul>
	        <% @person.errors.full_messages.each do |msg| %>
	          <li><%= msg %></li>
	        <% end %>
	      </ul>
	    </div>
  	<% end %>

	<%= f.text_field :name %>

	<%= f.form_group :gender, label: { text: "Gender" } do %>
  		<%= f.radio_button :gender, "Male", label: "Male", checked: true %>
  		<%= f.radio_button :gender, "Female", label: "Female" %>
	<% end %>

	<%= f.text_field :image_location %>
	
	<%= f.submit %>

<% end %>