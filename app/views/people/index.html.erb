<h1>True Facts About Session 2</h1>

<% num = Person.first.id + rand(Person.count) %>

<% link = "/people/#{num}/facts" %>

<h2><%= link_to 'Guess', link %>    |    
<%= link_to 'Add Person', new_person_path %></h2>

<ul>
	<% @people.each do |person| %>
		<li><%= link_to image_tag(person.image_location), person_path(person) %></li>
	<% end %>
</ul>

